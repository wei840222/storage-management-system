stages:
- name: push
  steps:
  - publishImageConfig:
      dockerfilePath: ./api/Dockerfile
      buildContext: ./api
      tag: storage-management-system-api
      pushRemote: true
      registry: index.docker.io
timeout: 60
branch:
  include:
  - master
notification: {}
